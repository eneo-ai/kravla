{
  "name": "@oddly-even/kravla-service",
  "version": "0.3.1",
  "description": "Headless HTTP crawl service wrapping @oddly-even/kravla — NDJSON streaming and signed-webhook delivery.",
  "license": "MIT",
  "type": "module",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Oddly-Even/kravla.git",
    "directory": "packages/service"
  },
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "build": "tsup",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@oddly-even/kravla": "workspace:*",
    "pino": "^10.3.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "tsup": "^8.5.0"
  },
  "engines": {
    "node": ">=20"
  }
}
