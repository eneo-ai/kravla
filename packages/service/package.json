{
  "name": "kravla-service",
  "version": "0.4.0",
  "description": "Headless HTTP crawl service wrapping kravla — NDJSON streaming and signed-webhook delivery.",
  "license": "AGPL-3.0-only",
  "type": "module",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/eneo-ai/kravla.git",
    "directory": "packages/service"
  },
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "build": "tsup",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "kravla": "workspace:*",
    "pino": "^10.3.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "tsup": "^8.5.0"
  },
  "engines": {
    "node": ">=20"
  }
}
